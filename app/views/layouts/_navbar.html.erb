<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
      <span class="sr-only">Toggle navigation</span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="#">Brand</a>
    </div>
    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li class="active"><%= link_to "Blog", posts_path %><span class="sr-only"></span></a></li>
        <li><a href="#">Link</a></li>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Dropdown <span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><a href=">Action</a></li>
            <li><a href="#">Another action</a></li>
            <li><a href="#">Something else here</a></li>
            <li role="separator" class="divider"></li>
            <li><a href=>Separated link</a></li>
            <li role="separator" class="divider"></li>
            <li><a href=>click on this link</a></li>
          </ul>
        </li>
      </ul>
      <form class="navbar-form navbar-left">
        <div class="form-group">
          <input type="text" class="form-control" placeholder="Search">
        </div>
        <button type="submit" class="btn btn-default">Submit</button>
      </form>
      <%= form_for(:user, :url => session_path(:user)) do |f| %>
      
      <ul class="nav navbar-nav navbar-right">
        <% if !current_user %>
        <li><p class="navbar-text">目前還未登入!</p></li>
        <% elsif  %>
        <li><p class="navbar-text"><%= current_user.email %></p></li>
        <% end %>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown"><b>Login</b> <span class="caret"></span></a>
          <ul id="login-dp" class="dropdown-menu">
            <li>
              <div class="row">
                <div class="col-md-12">
                  Login via
                  <div class="social-buttons">
                    <a id="checkLoginState" onclick="checkLoginState();" class="btn btn-fb"><i class="fa fa-facebook"></i> Facebook</a>
                    <a href="#" class="btn btn-tw"><i class="fa fa-twitter"></i> Twitter</a>
                  </div>
                  or
                  <form class="form" role="form" method="post" action="login" accept-charset="UTF-8" id="login-nav">
                    <div class="form-group">
                      <%= f.text_field :email, class: "form-control", placeholder: "Email address" %>
                      
                      <div class="form-group">
                        <%= f.password_field :password, class: "form-control", placeholder: "Password "%>
                      </div>
                      <% if !current_user %>
                      <div class="help-block text-right"><a href=""><%= link_to "忘記密碼?", new_password_path(:user) %></a></div>
                      <% elsif %>
                      <img class="mx-auto rounded-circle" src="/assets/me.jpg" >
                      <% end %>
                    </div>
                    <div class="form-group">
                      <%= f.submit '登入', class: "btn btn-primary btn-block" %>
                      
                    </div>
                    <div class="form-group">
                      <%= link_to "登出", destroy_user_session_path, method: :delete, class: "btn btn-danger btn-block" %>
                    </div>

                    <div class="form-group">
                      <a id="fbLogout" onclick="fbLogout()" class="btn btn-primary btn-block">登出臉書帳號</a>
                    </div>

                      <label>
                        <%= f.check_box :remember_me %> keep me logged-in.
                      </label>
                    </div>
                  </form>
                </div>
                <% if !current_user %>
                <div class="bottom text-center">
                  還沒有帳號 ? <a href="#"><b><%= link_to("註册", new_user_registration_path) %></b></a>
                </div>
                <% elsif  %>
                <div class="bottom text-center">
                  <% end %>
                  <div><%= link_to "Sign in with Facebook", user_facebook_omniauth_authorize_path %></div>
                    
                    
                    <div class="checkbox">
                </div>
              </div>
            </li>
          </ul>
        </li>
      </ul>
      </div><!-- /.navbar-collapse -->
      </div><!-- /.container-fluid -->
    </nav>
    <% end %>